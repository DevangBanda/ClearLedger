apiVersion: apps.kruise.io/v1beta1
kind: CloneSet
metadata:
  name: kyc
  namespace: clearledger
spec:
  replicas: 6

  selector:
    matchLabels:
      app: kyc

  template:
    metadata:
      labels:
        app: kyc
    spec:
      containers:
        - name: kyc
          image: clearledger/clearledger-kyc:v1.0.3
          envFrom:
            - configMapRef:
                name: clearledger-config
            - secretRef:
                name: clearledger-secret
          ports:
            - name: http
            - containerPort: 8087
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

  updateStrategy:
    type: InPlaceIfPossible   # ✅ FIXED HERE

    rollingUpdate:
      partition: 4
      maxUnavailable: 1
      maxSurge: 1

---
apiVersion: v1
kind: Service
metadata:
  name: kyc
  namespace: clearledger
spec:
  selector:
    app: kyc
  ports:

    - port: 8087
      targetPort: 8087