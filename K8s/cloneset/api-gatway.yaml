apiVersion: apps.kruise.io/v1beta1
kind: CloneSet
metadata:
  name: api-gateway
  namespace: clearledger
spec:
  replicas: 6

  selector:
    matchLabels:
      app: api-gateway

  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: clearledger/clearledger-api-gateway:v1.0.3
          envFrom:
            - configMapRef:
                name: clearledger-config
            - secretRef:
                name: clearledger-secret
          ports:
            - name: http
            - containerPort: 8080
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

  updateStrategy:
    type: InPlaceIfPossible   # ✅ FIXED HERE

    rollingUpdate:
      partition: 4
      maxUnavailable: 1
      maxSurge: 1

---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: clearledger
spec:
  selector:
    app: api-gateway
  ports:

    - port: 8080
      targetPort: 8080