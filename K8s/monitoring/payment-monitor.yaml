apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payment-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: payment
  namespaceSelector:
    matchNames:
      - clearledger
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s