apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: auth-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: auth
  namespaceSelector:
    matchNames:
      - clearledger
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s