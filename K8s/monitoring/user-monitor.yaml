apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: user
  namespaceSelector:
    matchNames:
      - clearledger
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s