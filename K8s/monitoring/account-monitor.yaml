apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: account-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: account
  namespaceSelector:
    matchNames:
      - clearledger
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s