apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: admin-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: admin
  namespaceSelector:
    matchNames:
      - clearledger
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s